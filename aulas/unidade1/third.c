#include <stdio.h>

int main(){
    int n = 14;
    int m = 23;
    int s;

    printf("n = %i, m = %i, s = %i\n", n, m, s);

    s = n + m;

    printf("n = %i, m = %i, s = %i\n", n, m, s);

    return 0;
}